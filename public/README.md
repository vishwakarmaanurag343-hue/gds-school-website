# Public Assets

Store your static assets (images, icons, etc.) in this folder.
Files in this folder can be accessed directly via the URL.

Example:
If you place `logo.png` here, you can use it in your code like:
`<img src="/logo.png" />`
